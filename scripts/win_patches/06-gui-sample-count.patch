diff --git a/window.v b/window.v
index 7de5f09..276c06f 100644
--- a/window.v
+++ b/window.v
@@ -103,6 +103,7 @@ pub:
 	on_event            fn (e &Event, mut w Window) = fn (_ &Event, mut _ Window) {} // global event hook; fires for all events
 	log_level           log.Level                   = default_log_level()
 	debug_layout        bool // print layout timing stats to stdout each frame
+	sample_count        int  // MSAA samples (0/1 = off, 2–4 = antialiased lines/polygons)
 }
 
 fn default_log_level() log.Level {
@@ -130,6 +131,7 @@ pub fn window(cfg &WindowCfg) &Window {
 		bg_color:                     cfg.bg_color.to_gx_color()
 		width:                        cfg.width
 		height:                       cfg.height
+		sample_count:                 cfg.sample_count
 		window_title:                 cfg.title
 		event_fn:                     event_fn
 		enable_dragndrop:             cfg.dragndrop
