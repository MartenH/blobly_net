diff --git a/vlib/v/gen/c/autofree.v b/vlib/v/gen/c/autofree.v
index 6799cb1..1feb4f7 100644
--- a/vlib/v/gen/c/autofree.v
+++ b/vlib/v/gen/c/autofree.v
@@ -39,7 +39,7 @@ fn (mut g Gen) autofree_scope_vars_stop(pos int, line_nr int, free_parent_scopes
 	}
 	g.trace_autofree('// autofree_scope_vars(pos=${pos} line_nr=${line_nr} scope.pos=${scope.start_pos} scope.end_pos=${scope.end_pos})')
 	g.autofree_scope_vars2(scope, scope.start_pos, scope.end_pos, line_nr, free_parent_scopes,
-		stop_pos)
+		stop_pos, pos)
 }
 
 @[if trace_autofree ?]
@@ -57,7 +57,7 @@ fn (mut g Gen) print_autofree_var(var ast.Var, comment string) {
 }
 
 fn (mut g Gen) autofree_scope_vars2(scope &ast.Scope, start_pos int, end_pos int, line_nr int, free_parent_scopes bool,
-	stop_pos int) {
+	stop_pos int, cur_pos int) {
 	if scope == unsafe { nil } {
 		return
 	}
@@ -66,6 +66,13 @@ fn (mut g Gen) autofree_scope_vars2(scope &ast.Scope, start_pos int, end_pos int
 		match obj {
 			ast.Var {
 				g.trace_autofree('// var "${obj.name}" var.pos=${obj.pos.pos} var.line_nr=${obj.pos.line_nr}')
+				if cur_pos > 0 && obj.pos.pos > cur_pos {
+					// Do not free vars declared after the cleanup point (e.g. an
+					// early `return`/`continue` mid-scope): in C they are declared
+					// further down, so freeing them here references an undeclared var.
+					g.trace_autofree('// skipping var "${obj.name}" declared after cleanup pos ${cur_pos}')
+					continue
+				}
 				if obj.name in g.returned_var_names {
 					g.print_autofree_var(obj, 'returned from function')
 					g.trace_autofree('// skipping returned var')
@@ -137,7 +144,8 @@ fn (mut g Gen) autofree_scope_vars2(scope &ast.Scope, start_pos int, end_pos int
 	if free_parent_scopes && scope.parent != unsafe { nil } && !scope.detached_from_parent
 		&& (stop_pos == -1 || scope.parent.start_pos >= stop_pos) {
 		g.trace_autofree('// af parent scope:')
-		g.autofree_scope_vars2(scope.parent, start_pos, end_pos, line_nr, true, stop_pos)
+		g.autofree_scope_vars2(scope.parent, start_pos, end_pos, line_nr, true, stop_pos,
+			cur_pos)
 	}
 }
 
diff --git a/vlib/v/parser/assign.v b/vlib/v/parser/assign.v
index 7dc84e3..1b2d816 100644
--- a/vlib/v/parser/assign.v
+++ b/vlib/v/parser/assign.v
@@ -252,7 +252,7 @@ fn (mut p Parser) partial_assign_stmt(left []ast.Expr) ast.Stmt {
 					} else if p.prev_tok.kind == .rsbr {
 						v.typ = ast.array_type_idx
 					}
-					if p.pref.autofree && right.len > 0 {
+					if (p.pref.autofree || p.pref.gc_mode == .boehm_leak) && right.len > 0 {
 						expr_for_or := if v.expr !is ast.EmptyExpr {
 							v.expr
 						} else {
